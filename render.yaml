services:
  - type: web
    name: rockfall-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.mainrf:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0